import R from 'ramda';

import { getters } from '@/store/editor';
import { defaultLaneStyle } from '@/utils/themeConst';

describe('editor/index.js', function () {
  // const
  //   style1 = R.merge(defaultLaneStyle, { width: 30 }),
  //   style2 = R.merge(defaultLaneStyle, { width: 60 }),
  //   themes = { currStyle: { laneStyles: [style1, style2] } },
  //   editorState = { themes };
  //
  // it('canvasInfo', function () {
  //   const canvasInfo = getters.canvasInfo(editorState);
  //   expect(canvasInfo).to.have.deep.property('laneXList', [0, 30, 90]);
  // });
});
